<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moshesteinvortzel.assaftayouri.ihw">

    <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GUI.Activities.AddNewCourseActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GUI.Activities.CreateAccountActivity" />
        <activity android:name=".GUI.Activities.LoginActivity" />
        <activity android:name=".GUI.Activities.HoursclassfragActivity" />
        <activity android:name=".GUI.Activities.AddActivity" />
        <activity android:name=".GUI.Activities.MainActivity" />

        <receiver
            android:name=".LOGIC.Secondary.NotificationReceiver"
            android:enabled="true"
            android:exported="true"></receiver>
    </application>

</manifest>